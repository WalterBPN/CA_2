package CA_2;


/**
 * Console menu options required by the brief.
 */

public enum MenuOption {
    SORT,               // sort applicants and print the first 20
    SEARCH,             // search a name and show role + department
    ADD_RECORD,         // add a new employee
    CREATE_BINARY_TREE, // build hierarchy and show height + count
    EXIT
}